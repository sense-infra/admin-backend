# SenseGuard Security Platform Environment Configuration
# Copy this file to .env and update the values for your environment

# Server Configuration
SERVER_PORT=8080
ENVIRONMENT=development

# Database Configuration
DB_HOST=dev-mariadb-ms.dev
DB_PORT=3306
DB_USER=admin
DB_PASSWORD=changeme
DB_NAME=dev
DB_SSLMODE=false

# JWT Configuration (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=your_super_secret_jwt_key_here_change_in_production

# Security Configuration
SESSION_TIMEOUT_HOURS=24
MAX_FAILED_LOGINS=5
ACCOUNT_LOCK_TIME_MINUTES=30
REQUIRE_HTTPS=false
CSRF_PROTECTION=true
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS_PER_MINUTE=60

# Default Admin Credentials (CHANGE THESE!)
# Username: admin
# Password: SenseGuard2025!
# Email: admin@senseguard.local

# Default Viewer Credentials (CHANGE THESE!)
# Username: viewer  
# Password: Viewer2025!
# Email: viewer@senseguard.local

# Production Security Notes:
# 1. Change all default passwords immediately
# 2. Use a strong, unique JWT_SECRET
# 3. Enable HTTPS in production (REQUIRE_HTTPS=true)
# 4. Use strong database credentials
# 5. Consider using environment-specific secret management
# 6. Enable proper firewall rules
# 7. Regular security updates and monitoring
